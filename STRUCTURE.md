# 项目结构概览

```
dom-toolkit/
├── 📁 src/                    # 源代码
│   ├── index.js               # 统一API入口
│   ├── coordinate.js          # 坐标显示模块
│   ├── detector.js            # 元素检测模块
│   ├── interactor.js          # 交互操作模块
│   └── scroller.js           # 滚动控制模块
├── 📁 dist/                   # 构建输出
│   ├── index.js              # 完整工具包
│   └── modules/              # 独立模块
│       ├── coordinate.min.js
│       ├── detector.min.js
│       ├── interactor.min.js
│       └── scroller.min.js
├── 📁 examples/               # 使用示例
│   ├── cdn-demo.html        # CDN使用演示
│   ├── basic-usage.js        # 基础用法示例
│   └── advanced-usage.js     # 高级用法示例
├── 📁 tests/                  # 测试文件
│   ├── full-test.html        # 完整功能验证
│   └── quick-test.html       # 快速功能测试
├── 📁 docs/                   # 文档
│   ├── AGENTS.md             # 开发者指南
│   ├── CHANGELOG.md           # 版本更新日志
│   └── PROJECT.md            # 项目总览
├── 📁 config/                 # 构建配置
│   ├── README.md             # 配置说明
│   ├── vite.config.js         # 主库构建
│   ├── vite.inject.config.js  # 注入版本构建
│   ├── vite.*.config.js      # 各模块独立构建
├── 📄 README.md               # 用户文档
├── 📄 package.json            # 项目配置
├── 📄 .gitignore              # Git忽略规则
└── 🚀 start-test-server.sh    # 测试服务器启动脚本
```

## ✅ 标准化改进

### 文件组织优化：
- ✅ 测试文件移至 `tests/` 目录
- ✅ 文档文件移至 `docs/` 目录  
- ✅ 构建配置移至 `config/` 目录
- ✅ 保持根目录简洁

### 项目结构优点：
- 🎯 **职责清晰**：每个目录有明确用途
- 📁 **易于维护**：文件分类合理，便于查找
- 🚀 **便于部署**：dist目录独立，方便CDN使用
- 📚 **文档完整**：docs目录统一管理所有文档
- 🧪 **测试规范**：tests目录专门存放测试文件

### 开发工作流：
```bash
# 启动开发环境
./start-test-server.sh

# 访问测试页面
http://localhost:8080/tests/full-test.html

# 构建项目
npm run build:all
```

现在项目结构更加规范和专业！